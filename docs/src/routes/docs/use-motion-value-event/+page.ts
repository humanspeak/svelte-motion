import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'useMotionValueEvent',
    description: 'Subscribe to motion value store changes with automatic cleanup.'
})
