import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'useMotionTemplate',
    description:
        'Compose reactive CSS strings from multiple stores using a tagged template literal.'
})
