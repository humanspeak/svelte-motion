import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'Svelte Animations',
    description:
        'Every Svelte 5 animation pattern in one place — gestures, exit animations, layout, variants, drag, scroll-linked motion values, SVG paths, and hooks. Built with @humanspeak/svelte-motion.'
})
