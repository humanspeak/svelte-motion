import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'Compare',
    description:
        'See how @humanspeak/svelte-motion compares to Framer Motion, Motion One, and GSAP. Honest, side-by-side comparisons with feature matrices, pros / cons, and verdicts.'
})
