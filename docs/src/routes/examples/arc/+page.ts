import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
    title: 'arc()',
    description:
        'Curved transition.path examples for keyframes, tangent-following rotation, and shared layout.'
})
