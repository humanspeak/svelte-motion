> **CLOSED 2026-08-19** — 1/1 plans DONE. Guard verdict: **PASS** at `7498c80`
> (check 0 errors · 830/830 unit · build+publint · trunk · 5/5 e2e). Delivered:
> `arc` export + `ArcOptions`/`MotionPathDefinition`/`PathInterpolator`/`PathState`/`Point2D`
> types, `_MotionContainer.arc.spec.ts`, `/tests/arc/{keyframes,layout}` routes,
> 2 e2e specs, `/docs/arc` + `/examples/arc` (3 demos), nav/index/compare rows,
> README bullet, minor changeset. Remaining for operator: live-demo sign-off,
> then push + PR (held per standing instruction).

# Implementation Plans — arc-motion-path

Generated by the improve skill on 2026-08-19 (`plan` variant, single plan).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

Initiative: close the `arc()` curved-motion-path parity gap vs upstream Motion
13.x / motion-sv 0.1.13 (`.competitive-intel/state.json` → `open_gaps[0]`).
Planned against commit `526f503` on branch `feat/arc`.

## Execution order & status

| Plan | Title                                                                          | Priority | Effort | Depends on | Status |
| ---- | ------------------------------------------------------------------------------ | -------- | ------ | ---------- | ------ |
| 001  | Ship `arc()` curved motion paths (`transition.path`) with docs, demos, tests   | P1       | M      | —          | DONE — guard PASS at `7498c80` (2026-08-19); PR held for operator sign-off |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- None. Single plan.

## Key recon facts (so nobody re-derives them)

- `motion-dom@13.0.0` (installed) already ships `arc` and the full
  `transition.path` runtime wiring; `motion@13.1.0`'s root entry does NOT
  export `arc` — re-export from `motion-dom`.
- Plan-time probe through `_MotionContainer.svelte` confirmed
  `transition={{ path: arc(...) }}` already curves `x`/`y`, composes
  `pathRotation` onto `transform`, and settles exactly. The plan is therefore
  export + docs/demos + regression tests + compare rows, not a runtime port.

## Findings considered and rejected

- Re-porting upstream `arc.ts` + its 15 geometry tests into `src/lib`: rejected —
  we consume the built `motion-dom`; duplicating the math would drift from
  upstream and double-maintain. Test our integration instead.
- Widening `MotionTransition` / `src/lib/types.ts` for `path`: rejected —
  motion's `ValueTransition` already declares `path?: MotionPath`.
- Bumping `motion-dom` to 13.1.x as part of this work: rejected — not needed
  for `arc`; keep the dependency change separate.
- Editing `.competitive-intel/state.json` to close the gap: rejected — the
  nightly digest derives closed gaps from code evidence.
