# Guard log — 001 typed-lint-promise-rules

## Checkpoint 1 — 2026-07-09 17:20 — DRIFTING

`2b37049` · final close-out pass over the whole branch (`main...chore/typed-lint-recommended`, 4 commits, 80 files). Working tree was clean at checkpoint — nothing to snapshot-commit.

Substance is strong: the plan's intent is delivered and exceeded (zero new `trunk-ignore`s; every library-source `any`-bridge got a real type). All gates reproduced independently, not taken on report. Two done criteria nonetheless do not hold as written.

- **Criterion 6 FAILS** — "Every `void`-marked promise … has a one-line rationale comment." Three lack one, all `void unmount(...)`: `src/lib/utils/animateView.spec.ts:73`, `src/lib/utils/hookVanillaParity.svelte.spec.ts:151`, `:166`. Classification: executor drift (minor, mechanical).
- **Load-bearing config comment is factually wrong** — `eslint.config.mjs:117-138` justifies the repo-wide `no-unnecessary-type-assertion` off for `**/*.svelte` with "19 such false positives … The `as`-style assertions the rule found in `.svelte` files were real and are removed." Forcing the rule back on (`npx eslint src/lib/html/_MotionContainer.svelte --rule '{"@typescript-eslint/no-unnecessary-type-assertion":"error"}'`) reports **45 distinct flagged lines in that file alone — 31 `!`-style and 14 `as`-style still present** (`:1501`, `:2189`, `:2358`, `:2499`, `:2784`, `:2822`, `:2849`, …). The "19" is really the count of `pnpm check` errors the autofix produced, per commit `8e177d4`'s own body; the comment conflates the two. Mechanism claim (svelte-eslint-parser narrows `$state` across closures, svelte-check does not) is sound. Classification: executor drift — the standing justification a future maintainer will read is inaccurate.
- **Criterion 10 FAILS, but as a plan defect, not drift** — `.coderabbit.yaml` is out of the plan's exhaustive in-scope list. The edit (`labels: ['coderabbit']` → `[coderabbit]`) is _forced_ by criterion 4: `.trunk/configs/.yamllint.yaml` sets `quoted-strings: required: only-when-needed`, so `trunk check --all` cannot exit 0 with the quoted form. The plan's in-scope list and its repo-wide-green criterion contradict each other. Executor disclosed it (`2b37049`: "one yamllint quoting nit"). Action: operator decides — accept and amend scope, or revert.
- **Plan defect — bad drift baseline** — `Planned at 8491bd3` is not an ancestor of this branch; it sits on `feat/svg-motion-value-attributes`. Branch base is `a023d51` (main). The plan's prescribed drift check therefore diffs against a sibling feature branch. Harmless in the event: `git show main:eslint.config.mjs` line 31 is `...ts.configs.recommended,`, matching the plan's "Current state" excerpt, so proceeding was correct.
- **Accepted risk, permitted by criterion 5** — `no-unsafe-assignment` is now off for **all** `**/*.svelte`, including library source, against the plan's stated "Library source (src/lib non-spec) keeps these rules." Forced the rule back on across `src/lib/**/*.svelte` with a validated instrument: **0 hits**, so nothing is hidden today. Rail is down for future library `.svelte` code. Disclosed with rationale + count in `2b37049`.
- **No gamed criteria** — no assertion removed (the `-1` in `variants.spec.ts` is a reformat of a multi-line `expect(...)` onto one line), no `.skip`/`.only`/`.todo` added; the `as never` removals _strengthen_ typing. Real latent bugs fixed in library source and verified by reading: `layout.ts:43` now `.catch(() => undefined)`s the WAAPI cancel rejection before `void`; `optimizedAppear.ts` truthiness-on-Promise feature detection; `svg.ts` / `motionTemplate.svelte.ts` `[object Object]` writes; `AnimatePresence.svelte` + `PresenceChild.svelte` `$props<T>()` (Svelte 4 form) left every prop `any`.
- Gates reproduced: `trunk check --all` exit 0 · `trunk check` "Checked 80 modified files ✔ No issues" · `pnpm check` 0 errors / 1013 files · `pnpm test:only` exit 0 · `pnpm exec playwright test --list` 319 tests in 86 files · `grep -rn "eslint-disable" src e2e` → nothing.
- Step 3's four-path guard independently re-verified without recreating scratch files: forced a type-aware rule on one file per parser path; a typed rule crashing with "You have used a rule which requires type information" is the tell for a dead path. `src/lib/utils/svg.ts` (src `.ts`), `projection.spec.ts` (spec `.ts`, 17 hits), `_MotionContainer.svelte` (`.svelte`, 45 hits), `e2e/view/basic.spec.ts` (e2e `.ts`) all ran clean. Negative control `playwright.config.ts` **threw** that exact error — confirming the probe detects a missing path, and that root config files correctly land in the `disableTypeChecked` guard.
- Action: reported to operator; **no PR opened** (NO-PASS, and the plan's Git workflow forbids a PR without explicit instruction). Close-out report written to `001-typed-lint-promise-rules.guard-report.md`.

## Checkpoint 2 — 2026-07-09 18:40 — PLAN AMENDED

`21d869c` · plan amendment, re-applied after loss. Operator accepted `.coderabbit.yaml` (F3). An earlier amendment was written but never committed — the hook aborted guard's commit, and the executor's `21d869c` then cleaned the working tree, discarding guard's plan/log edits while absorbing guard's `README.md` status row into its own commit. Re-applied here.

- **Scope amended**: `.coderabbit.yaml` admitted to the in-scope list, quoting changes only. Criterion 4 (`trunk check --all` exits 0) and criterion 10 (only in-scope files) contradicted each other, because `.trunk/configs/.yamllint.yaml` sets `quoted-strings: required: only-when-needed`. The plan was wrong about reality; the work was not wrong about the plan. Criterion 10 now reads as met.
- **Baseline amended**: `Planned at` re-stamped `8491bd3` → `21d869c`. The old SHA is not an ancestor of this branch (it lives on `feat/svg-motion-value-attributes`), so the prescribed drift check compared against unrelated work.
- **Third plan defect found and fixed**: the plan file itself, committed at `561c9b8`, violates `prettier` and `markdownlint/MD060`, so `trunk check --all` could never exit 0 while it existed — the plan made its own done criterion unsatisfiable, and the executor is forbidden from editing the plan to fix it. Guard ran `trunk fmt` over the plan and guard artifacts. Formatting only. Evidence: `trunk check --all` reported `.agents/.plans/typed-lint/001-typed-lint-promise-rules.md:158 fmt + markdownlint/MD060`, exit 1; after `trunk fmt`, exit 0.
- **Guard self-correction**: checkpoint 1 asserted "`trunk check --all` exit 0". That was never verified — the command piped into `tail` and then `echo`, so `$?` captured the echo. Re-run with the exit code read directly, `trunk check --all` exited **1** at `21d869c` and (identical plan blob `4068732`) at `2b37049` too. Both executor commit bodies claim exit 0; that claim was false, and guard repeated it. Corrected in the close-out report.
- Action: plan, batch README, and this log amended with operator agreement. Proceeding to close-out.

## Checkpoint 3 — 2026-07-09 18:40 — ON TRACK

`21d869c` · close-out re-run after the executor's remediation of F1 and F2. Source tree clean at checkpoint; nothing to snapshot-commit.

- **F1 CLOSED** — all three `void unmount(...)` sites now carry a rationale (`animateView.spec.ts:75`, `hookVanillaParity.svelte.spec.ts:153`, `:170`). Audited every statement-position `void` in `src/`: 23 total, all 8 introduced by this branch carry a rationale. The 3 un-annotated `void` promises (`transform-template/+page.svelte:98`, `transform-template/stop/+page.svelte:49`, `animate-presence/basic/+page.svelte:84`) are pre-existing on `main`, in files this branch never touches; annotating them would exceed the plan's "minimal, mechanical fixes only". Criterion 6 met for this work.
- **F2 CLOSED, and it was a real defect** — the executor removed 11 genuinely-unnecessary `as`-style assertions from `_MotionContainer.svelte` and rewrote the comment against re-measured evidence. Verified column-accurately (`--rule` override, reading the reported column rather than the line text): **35 flagged, all 35 non-null `!`, 0 `as`-style**, all in one file. Matches the new comment exactly. `pnpm check` stays at 0 errors with those 11 removed, confirming they were true positives and the old comment's "they were real and are removed" was premature.
- **Guard self-correction** — checkpoint 1's "14 `as`-style still present" was inflated by a line-based classifier that could not tell which assertion on a line was flagged (e.g. `animate(element!, snapshot as DOMKeyframesDefinition)` flags the `!`, not the `as`). The true figure was the executor's 11. The finding's substance held — `as`-style assertions did exist, contrary to the comment — but guard's count was wrong and is corrected here.
- **Not reproduced**: the new comment's claim that applying all 35 fixes yields 19 svelte-check errors. Verifying it requires mutating source. It is consistent with `8e177d4`'s body and is illustrative rather than load-bearing; the rule-off rests on "some of the 35 break the build", which `8e177d4` demonstrated.
- Gates re-run at `21d869c`, exit codes read directly: `trunk check --all` exit 0 (after the plan-file format) · `trunk fmt && trunk check` exit 0 · `pnpm check` 0 errors / 1013 files · `pnpm test:only` 752/752 · `pnpm exec playwright test --list` 319 tests / 86 files · `grep -rn "eslint-disable" src e2e` → nothing · `no-unsafe-assignment` forced on across `src/lib/**/*.svelte` → 0 hits.
- Scope: outside `src/` and `e2e/`, the branch touches only `eslint.config.mjs`, `.coderabbit.yaml` (now in scope), and the plan/guard artifacts. No `docs/**`, `tsconfig.json`, `.trunk/trunk.yaml`, or `package.json`.
- **Verdict: PASS.** Action: close-out report rewritten. PR **not** opened — the plan's Git workflow states "Do NOT push or open a PR unless instructed. The maintainer signs off live before any PR (house rule)." Guard does not force past that guardrail; handed back to the operator for an explicit go.

## Checkpoint 4 — 2026-07-09 18:51 — ON TRACK

`e8e050c` · guard self-correction. Verdict unchanged: **PASS**. Source tree clean; no source touched.

- **Retracted a false residual-risk finding.** Checkpoint 3's report claimed `.husky/pre-commit`'s `trunk check --fix` rewrote `src/lib/html/_MotionContainer.svelte`, stripping `element!` despite `no-unnecessary-type-assertion` being `off` for `**/*.svelte`, and inferred that Trunk's sandbox ignores the rule-off. **False, and never reproduced.** Controlled test on a clean tree: `trunk check --fix` (the hook's exact command, no path args) → exit 0, "Checked 82 modified files ✔ No issues", `_MotionContainer.svelte` blob unchanged at `e1e1460` before and after, `git status` clean. Trunk honors the rule-off.
- **Correct cause:** a concurrent executor session was writing to that file during guard's commit, running the raw `eslint --fix` measurement that `21d869c` documents (strip all 35 `element!`, count 19 svelte-check errors). Trunk's `discarded as modified during check [eslint]` means it detected the file mutating _while it checked_ and discarded its results — trunk behaving correctly under a racing writer — and the non-zero exit aborted the commit. Guard read "modified during check" as "trunk modified it".
- **Root cause in guard's own process:** an inference (`the hook must have done it`) was written into the report as an observation. This is the second such lapse this session, after the unverified `trunk check --all` exit code. Both are the same failure the plan's own reviewer note warns about: assert only what you reproduced.
- **Downgraded, not deleted:** the hook does run `trunk check --fix` with no path args and restages only originally-staged files, so a fix to an unstaged file would leak into the working tree. Latent, currently inert (nothing to fix), out of plan scope. Removing `--fix` — the operator's first instinct given guard's bad finding — would make autofixable nits hard commit failures and is **not** recommended.
- Action: report corrected, retraction recorded here rather than overwriting the original claim. Operator instructed guard to open the PR; proceeding under that explicit instruction, which satisfies the plan's "unless instructed" clause.
