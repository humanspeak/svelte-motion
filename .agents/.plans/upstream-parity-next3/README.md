# Implementation Plans — upstream-parity-next3

Generated by the improve skill on 2026-07-23 (commit `0058f78`, the merged tip
of PR #450 / style/brutalist-examples). The next three upstream-parity items,
selected from the evidence trail of the closed `upstream-fidelity` batch
(see `.agents/.plans-closed/upstream-fidelity/README.md` — all three were
recorded there as discovered follow-ups with file-level evidence). Execute in
the order below. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

**Repo-wide execution rules** (also in every plan):

- Red test first, committed separately (`test(...)` then `fix(...)`).
- Lint/format via `trunk check` / `trunk fmt` (trunk.yaml is the authority).
- E2e: NEVER kill or reuse port 4198 (maintainer's live server). Edit
  playwright.config.ts's three 4198 refs to your assigned port locally,
  uncommitted; kill only YOUR port's stale listener before reruns.
- Executor worktrees are created from main — first action is always
  `git switch -c exec/<plan> <base>` per the dispatch instructions.
- Do NOT push.

## Execution order & status

| Plan | Title                                                               | Priority | Effort | Depends on                 | Status                                                                                                          |
| ---- | ------------------------------------------------------------------- | -------- | ------ | -------------------------- | --------------------------------------------------------------------------------------------------------------- |
| 001  | Gesture channels ride persistent MotionValues (velocity continuity) | P1       | M–L    | —                          | DONE — guard-approved; integrated (361c912 test + 6cc5c82 fix + 2f22db0 docs example)                           |
| 002  | First-ever controls.start() animates from the non-neutral idle      | P1       | S–M    | —                          | DONE — guard-approved; integrated (10f8ff2 test + 0f8055c fix); docs beam demo verified, no docs changes needed |
| 003  | Wildcard/relative keyframes resolve against the live value          | P2       | S–M    | 002 (same-file sequencing) | IN PROGRESS — dispatched to opus executor on post-002 tip                                                       |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 001 is independent (hover/interaction) and can run in parallel with 002.
- 002 → 003 sequentially: both edit `_MotionContainer.svelte` (render ternary
  vs. start-payload path).
- If 001 lands before 003, plan 003 prefers gesture MotionValues as the live
  source for channels they own (noted in its Current state).

## Selection rationale (and what was NOT picked)

- All three are true parity gaps with upstream evidence: velocity continuity
  is the scoped core of issue #449 (five audit findings traced to it);
  the first-start wipe was proven with diagnostics by fidelity plan 005's
  executor; wildcard landing was proven by fidelity plan 007's probes.
- **Not picked — reorder mid-drag scroll anchor**: upstream framer-motion has
  the same limitation and the maintainer explicitly ruled "if it is upstream
  we can keep it" (2026-07-23). Remains recorded in the closed fidelity index.
- **Not picked — full #449 VisualElement adoption**: plan 001 deliberately
  slices the highest-value part; the wholesale rewrite stays a standalone
  initiative.

## Audit provenance

No new audit sweep was run: the three findings carry forward from the
three-agent fidelity audit (commit `6746859`) plus executor discoveries during
that batch's execution, all recorded with `file:line` evidence at close-out.
Plan excerpts were re-read from the live code at `0058f78` by the advisor.
