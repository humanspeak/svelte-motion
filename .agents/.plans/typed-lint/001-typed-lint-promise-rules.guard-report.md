# Guard report — 001 typed-lint-promise-rules

**Recommendation: NO-PASS** — the work is substantively excellent and every gate reproduces green, but two done criteria fail as written and a config comment that justifies disabling a rule repo-wide is factually wrong.
**Reviewed at** `2b37049` · 2026-07-09 17:20 · **Plan planned at** `8491bd3`

Three small fixes flip this to PASS; none touch the design. See "What flips this to PASS".

## Done criteria

| Criterion                                                                                                                                  | Result                 | Evidence                                                                                                                                                                                |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `eslint.config.mjs` spreads `recommendedTypeChecked`; contains blocks (b), (c), (d); `docs/**` matches only the `disableTypeChecked` guard | met (deviation)        | `eslint.config.mjs:31`, `:90-108` (b), `:109-115` (c), `:145-158` (d). Block (b) drops `allowDefaultProject`; `*.ts` + `scripts/**/*.ts` moved into (c). `docs/**` gets no typed rules. |
| `e2e/tsconfig.json` exists and `playwright test --list` exits 0                                                                            | met                    | File present; `Total: 319 tests in 86 files`, exit 0                                                                                                                                    |
| Step 3's four-path guard run, all four flagged, scratch files deleted                                                                      | met                    | Recorded in `0a7b9df`. Independently re-verified (see Spirit). No scratch files in tree.                                                                                                |
| `trunk fmt && trunk check` exits 0 **and** `trunk check --all` exits 0                                                                     | met                    | `trunk check` → "Checked 80 modified files ✔ No issues"; `trunk check --all` → exit 0                                                                                                   |
| ONLY Step 2(d)'s six rule-offs; any additional listed in the report with rationale **and count**                                           | **met, count wrong**   | Exactly two additional offs, both `**/*.svelte` (`eslint.config.mjs:141-142`). Rationale given, but the stated count is wrong — see F2.                                                 |
| Every `void`-marked promise and new library `trunk-ignore` has a one-line rationale                                                        | **FAIL**               | `animateView.spec.ts:73`, `hookVanillaParity.svelte.spec.ts:151`, `:166` — bare `void unmount(...)`, no comment. (Zero new `trunk-ignore`s, so that half is vacuously met.)             |
| `grep -rn "eslint-disable" src e2e` returns nothing                                                                                        | met                    | no output                                                                                                                                                                               |
| `grep -rn "trunk-ignore(...no-explicit-any)" src/lib/utils/style.spec.ts` returns nothing                                                  | met                    | no output                                                                                                                                                                               |
| `pnpm check` → 0 errors; `pnpm test:only` → all pass                                                                                       | met                    | `COMPLETED 1013 FILES 0 ERRORS`; `pnpm test:only` exit 0                                                                                                                                |
| Only in-scope files modified                                                                                                               | **FAIL (plan defect)** | `.coderabbit.yaml` touched — but forced by the criterion above it; see F3                                                                                                               |
| `.agents/.plans/typed-lint/README.md` status row updated                                                                                   | met                    | Row 001 → `DONE`, with deviations recorded                                                                                                                                              |

## Spirit

The plan's `Why this matters` is delivered, and in the places that count the executor went past the plan rather than around it. Type-aware linting is genuinely live on all four parser paths — I re-verified this without recreating the scratch files, by forcing a type-aware rule onto one file per path and using the fact that a typed rule on a type-info-less file _throws_ ("You have used a rule which requires type information"). `src/lib/utils/svg.ts`, `projection.spec.ts` (17 hits), `_MotionContainer.svelte` (45 hits) and `e2e/view/basic.spec.ts` all ran without that error; the negative control, `playwright.config.ts`, threw it exactly as predicted.

The promise rules did what the plan hoped: they surfaced real latent bugs, and the executor fixed the bugs rather than silencing the rules. `layout.ts:43` had been dropping the promise from `animation.finished?.finally(...)`, so a WAAPI cancel rejection went unhandled — it now `.catch(() => undefined)`s before the `void`, and the optional chain correctly short-circuits the whole chain when `finished` is absent. `optimizedAppear.ts` was testing a Promise for truthiness as feature detection. `svg.ts` and `motionTemplate.svelte.ts` could each write a literal `[object Object]`. And `AnimatePresence.svelte` / `PresenceChild.svelte` were still using the removed Svelte 4 `$props<T>()` generic, meaning **every prop on both components was silently `any`** — a genuine typing hole the plan never anticipated. Zero new `trunk-ignore`s were added anywhere; the plan explicitly allowed suppression as a fallback for library-source `any`-bridges and the executor declined it.

Nothing was gamed. No assertion was deleted, no `.skip`/`.only`/`.todo` introduced, and the `as never` removals in `variants.spec.ts` _strengthen_ typing (the literal must now actually satisfy `Variants`). The one apparent `-1` in assertion count is an artifact of a multi-line `expect(...)` being reformatted onto a single line.

The gap is not in the code; it is in the record. The comment at `eslint.config.mjs:117-138` is the standing justification for turning a rule off across every `.svelte` file in the repo — the first thing a future maintainer reads before deciding whether the rail can come back up — and two of its load-bearing claims are false (F2). That is precisely the class of thing that ages into a wrong decision, so it should be corrected before merge rather than after.

## Scope & conduct

- **In-scope only?** No — one file: `.coderabbit.yaml`. Everything else lands in `src/`, `e2e/`, `eslint.config.mjs`, `e2e/tsconfig.json`, and the plan docs. The `.coderabbit.yaml` edit is a plan defect, not drift (F3).
- **STOP conditions respected?** Yes. Two were hit and correctly escalated rather than improvised past: `trunk check --fix` will not write to `.svelte` files (STOP: "trunk check lacks a --fix flag … the operator decides the autofix vehicle") → operator approved a one-time raw `npx eslint --fix`, disclosed in `8e177d4`; and Step 4's autofix proved **not** behavior-neutral (`querySelector(sel) as HTMLElement | null` supplies the generic `E`), which the executor pre-empted by rewriting 49 sites to `querySelector<HTMLElement>(sel)` rather than forcing green. Both are documented in the commit bodies. The `Planned at` drift check was unrunnable as written (F4) but the "Current state" excerpt it guards did match the branch base, so proceeding was correct.
- **Plan amendments during execution:** none. The plan file was not modified by the executor — no tampering. (`trunk fmt` reformatted it during _my_ review; I reverted it, tree is clean.)

## Findings

**F1 — Criterion 6 fails: three `void`s without a rationale. (executor drift; blocking)**
`src/lib/utils/animateView.spec.ts:73`, `src/lib/utils/hookVanillaParity.svelte.spec.ts:151` and `:166` are bare `void unmount(...)`. `unmount()` returns a Promise, which is why `no-floating-promises` flagged them and why the plan listed these exact three sites. The criterion says _every_ `void`-marked promise carries a one-line rationale. These are safe to not await (test teardown) — the fix is three comments, not a code change.

**F2 — The `.svelte` rule-off is justified by a false statement. (executor drift; blocking)**
`eslint.config.mjs:117-138` reads: _"Measured at the time of writing: 19 such false positives in `_MotionContainer.svelte`, 0 true positives left in `.svelte` after the one-time autofix. The `as`-style assertions the rule found in `.svelte` files were real and are removed."_ Re-enabling the rule on that one file reports **45 distinct flagged lines — 31 `!`-style and 14 `as`-style still present** (`:1501`, `:2189`, `:2358`, `:2399`, `:2499`, `:2522`, `:2545`, `:2784`, `:2822`, `:2843`, `:2849`, `:2907`, `:2920`, `:2926`). So the `as`-style claim is simply untrue, and "19" is in fact the number of `pnpm check` errors the autofix caused — commit `8e177d4`'s own body says so ("produced 19 'possibly null' errors"). The underlying _mechanism_ is real and I do not dispute the rule-off; only its stated evidence. Note this rule is hygiene-only: nothing unsafe can hide behind it.

**F3 — `.coderabbit.yaml` is out of scope, but the plan forced it. (plan defect; operator's call)**
`labels: ['coderabbit']` → `[coderabbit]`. `.trunk/configs/.yamllint.yaml` sets `quoted-strings: required: only-when-needed`, so the quoted form is a yamllint error; `trunk check` only inspects changed files, so `trunk check --all` (criterion 4) surfaced a pre-existing violation in a file the in-scope list does not permit touching. Criterion 4 and criterion 10 contradict each other. The executor took the green gate and disclosed the trade ("one yamllint quoting nit", `2b37049`). I did not amend the plan — that needs your yes.

**F4 — The plan's `Planned at` SHA is on the wrong branch. (plan defect; no action needed now)**
`8491bd3` is not an ancestor of this branch; it lives on `feat/svg-motion-value-attributes`. This branch was cut from `a023d51` (main). The plan's prescribed drift check therefore diffs source against a sibling feature branch and would show that branch's work as spurious drift. It happened to be harmless: `main:eslint.config.mjs` line 31 is `...ts.configs.recommended,`, matching the plan's "Current state" excerpt exactly. Worth fixing in the batch's next plan so the drift check means something.

## What flips this to PASS

1. Add a one-line rationale to the three `void unmount(...)` sites (F1).
2. Correct the comment at `eslint.config.mjs:117-138` (F2) — drop or fix the "`as`-style … are removed" sentence, and state the real flagged count (45 lines in `_MotionContainer.svelte`) rather than the 19 `pnpm check` errors. The `no-unsafe-assignment` half of that comment block is accurate and I confirmed it independently.
3. Tell me how to treat `.coderabbit.yaml` (F3): accept it and I amend the plan's scope with a dated revision note, or revert it and accept that `trunk check --all` is red on a pre-existing yamllint nit.

The executor need not re-run the gates for (1) and (2); they cannot affect behavior. I will re-checkpoint and re-run them anyway.

## Residual risk / follow-ups

- **A rail is down in library source.** `no-unsafe-assignment` is off for **all** `**/*.svelte` (`eslint.config.mjs:142`), not just tests — against the plan's stated "Library source (src/lib non-spec) keeps these rules." I forced the rule back on across `src/lib/**/*.svelte` with a validated instrument (control: the same override re-enabled a config-disabled rule and produced 17 hits elsewhere) and got **0 hits**, so nothing is hidden today. But future library `.svelte` code can accumulate genuinely unsafe assignments silently. Criterion 5 permits this given it was disclosed with a rationale, and it was. If the Trunk `$props()` type-info degradation is ever fixed upstream, this off should be revisited first.
- **Root configs and `scripts/**`get no type-aware linting.**`allowDefaultProject`cannot work under Trunk (it lints a sandbox copy whose path never matches a glob resolved against`tsconfigRootDir`); those files fell into the `disableTypeChecked`guard. I confirmed the mechanism directly — forcing a typed rule on`playwright.config.ts`throws "requires type information". This is outside the plan's`Why this matters` (library + e2e promise safety), so the coverage loss is acceptable, but it is a real boundary and belongs in the plan's Maintenance notes alongside the existing one about new top-level source directories.
- **README bookkeeping.** `.agents/.plans/typed-lint/README.md` says "three additional rule-offs" and then lists two; there are exactly two (`eslint.config.mjs:141-142`). The third item it seems to mean is the `disableTypeChecked` widening, which is already recorded as deviation 1.
- **Merge ordering.** Unchanged from the plan's dependency note: land this before `upstream-parity-top3` plans 003/004, which touch `drag.ts` and the projection/layout code this plan rewrote.
- **No PR opened.** NO-PASS earns none — and independently, the plan's Git workflow says "Do NOT push or open a PR unless instructed. The maintainer signs off live before any PR (house rule)." Even on a PASS I would have stopped here for your sign-off.
