# Guard report — 001 typed-lint-promise-rules

**Recommendation: PASS** — every done criterion re-run and reproduced green, both prior findings closed at the source rather than papered over, and the diff delivers the plan's intent with real latent bugs fixed.
**Reviewed at** `21d869c` · 2026-07-09 18:40 · **Plan planned at** `21d869c` (re-stamped; the original `8491bd3` was on a sibling branch)
**Integrated** — **no PR opened.** Not a preflight failure: the plan's own Git workflow says "Do NOT push or open a PR unless instructed. The maintainer signs off live before any PR (house rule)." Guard does not force past that guardrail. The work is committed on `chore/typed-lint-recommended` and awaits the operator's explicit go.

## Done criteria

| Criterion                                                                                                                                  | Result | Evidence                                                                                                                                                                                 |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `eslint.config.mjs` spreads `recommendedTypeChecked`; contains blocks (b), (c), (d); `docs/**` matches only the `disableTypeChecked` guard | met    | `eslint.config.mjs:31`, `:90-108` (b), `:109-115` (c), `:145-158` (d). Block (b) drops `allowDefaultProject` (deviation, disclosed); `docs/**` gets no typed rules.                      |
| `e2e/tsconfig.json` exists and `playwright test --list` exits 0                                                                            | met    | `Total: 319 tests in 86 files`, exit 0                                                                                                                                                   |
| Step 3's four-path guard run, all four flagged, scratch files deleted                                                                      | met    | Recorded in `0a7b9df`; independently re-verified at checkpoint 1 via typed-rule probes plus a negative control that threw "requires type information" on `playwright.config.ts`.         |
| `trunk fmt && trunk check` exits 0 **and** `trunk check --all` exits 0                                                                     | met    | Both exit 0, read directly (`echo $?`), after guard formatted the plan file. See F5 — this criterion was **failing** before that.                                                        |
| ONLY Step 2(d)'s six rule-offs; any additional listed in the report with rationale **and count**                                           | met    | Exactly two additional offs, both `**/*.svelte` (`eslint.config.mjs:141-142`), each with a rationale and a now-accurate count. Verified: 35 flagged, all non-null; 0 `as`-style.         |
| Every `void`-marked promise and new library `trunk-ignore` has a one-line rationale                                                        | met    | All 8 `void`s this branch introduces carry one. 23 statement-position `void`s audited across `src/`. Zero new `trunk-ignore`s exist. See F1.                                             |
| `grep -rn "eslint-disable" src e2e` returns nothing                                                                                        | met    | no output                                                                                                                                                                                |
| `grep -rn "trunk-ignore(...no-explicit-any)" src/lib/utils/style.spec.ts` returns nothing                                                  | met    | no output                                                                                                                                                                                |
| `pnpm check` → 0 errors; `pnpm test:only` → all pass                                                                                       | met    | `COMPLETED 1013 FILES 0 ERRORS`; `Tests 752 passed (752)`                                                                                                                                |
| Only in-scope files modified                                                                                                               | met    | Outside `src/`/`e2e/`: `eslint.config.mjs`, `.coderabbit.yaml` (admitted by the 2026-07-09 amendment), plan + guard artifacts. No `docs/**`, `tsconfig.json`, `.trunk/`, `package.json`. |
| `.agents/.plans/typed-lint/README.md` status row updated                                                                                   | met    | Row 001 present with deviations and guard verdict                                                                                                                                        |

## Spirit

The plan's `Why this matters` is delivered, and where it counts the executor went past the plan rather than around it. Type-aware linting is genuinely live on all four parser paths — verified independently by forcing a type-aware rule onto one file per path and exploiting the fact that a typed rule on a type-info-less file _throws_; the negative control (`playwright.config.ts`) threw exactly as predicted, so silence on the other paths is evidence of live type info rather than absence of a check.

The promise rules did what the plan hoped: they surfaced real latent bugs, and the executor fixed the bugs instead of silencing the rules. `layout.ts:43` was dropping the promise from `animation.finished?.finally(...)`, leaving a WAAPI cancel rejection unhandled; it now `.catch(() => undefined)`s before the `void`, and the optional chain short-circuits correctly when `finished` is absent. `optimizedAppear.ts` was testing a Promise for truthiness as feature detection. `svg.ts` and `motionTemplate.svelte.ts` could each write a literal `[object Object]`. And `AnimatePresence.svelte` / `PresenceChild.svelte` still used Svelte 4's removed `$props<T>()` generic, so **every prop on both components was silently `any`** — a typing hole the plan never anticipated. Zero `trunk-ignore`s were added anywhere, though the plan explicitly permitted them as a fallback.

Nothing was gamed. No assertion deleted, no `.skip`/`.only`/`.todo` added; the `as never` removals in `variants.spec.ts` _strengthen_ typing.

What most earns the PASS is how the two checkpoint-1 findings were closed. Neither was resolved by relaxing a criterion. The three `void unmount(...)` calls got real rationales. And the `.svelte` rule-off comment — the standing justification a future maintainer reads before deciding whether that rail can come back up — was not merely reworded: the executor discovered its "the `as`-style assertions were real and are removed" claim was false because those fixes had shared autofix hunks with the non-null removals that had to be reverted, then **removed the 11 surviving `as`-style assertions** and rewrote the comment against re-measured evidence, naming the command to reproduce it. `pnpm check` stays at 0 errors without them, proving they were true positives. The record now matches the code.

## Scope & conduct

- **In-scope only?** Yes, after the amendment. `.coderabbit.yaml` was the sole out-of-scope touch and is now admitted (F3). Everything else lands in `src/`, `e2e/`, `eslint.config.mjs`, `e2e/tsconfig.json`, and plan/guard artifacts.
- **STOP conditions respected?** Yes. Two were hit and escalated rather than improvised past: `trunk check --fix` will not write to `.svelte` files → operator approved a one-time raw `npx eslint --fix` (`8e177d4`); and Step 4's autofix proved **not** behavior-neutral (`querySelector(sel) as HTMLElement | null` supplies the generic `E`), pre-empted by rewriting 49 sites to `querySelector<HTMLElement>(sel)` rather than forcing green.
- **Plan amendments during execution:** one, 2026-07-09, by guard with operator agreement — three plan defects (F3, F4, F5). The executor never edited the plan file. It did edit the batch `README.md`, which the plan's executor instructions explicitly direct it to do.
- **One conduct note:** the executor's `21d869c` cleaned the working tree, discarding guard's uncommitted plan and log amendments while absorbing guard's `README.md` status row into its own commit. Not tampering — it did not edit the plan — but guard's amendment had to be re-applied. Worth a habit change: leave a dirty tree alone unless it is yours.

## Findings

**F1 — `void`s without a rationale. CLOSED in `21d869c`.**
Three `void unmount(...)` calls lacked the required one-line rationale. All three now carry one. Audited every statement-position `void` in `src/`: 23 total; all 8 the branch introduces are annotated. The three remaining un-annotated `void` promises (`transform-template/+page.svelte:98`, `transform-template/stop/+page.svelte:49`, `animate-presence/basic/+page.svelte:84`) are pre-existing on `main` in files this branch never touches — see Residual risk.

**F2 — The `.svelte` rule-off was justified by a false statement. CLOSED in `21d869c`.**
The old comment claimed the `as`-style assertions "were real and are removed" and cited "19 false positives". Neither held: 11 `as`-style assertions survived, and "19" was the svelte-check error count from `8e177d4`'s own body. Both are now fixed — the 11 are removed, and the comment is rewritten with the reproduction command, the true figure (35 flagged, all non-null `!`), and the two conditions under which the rail can come back up. Re-verified column-accurately: **35 flagged, 35 non-null, 0 `as`-style**, all in `_MotionContainer.svelte`.

**F3 — `.coderabbit.yaml` out of scope. RESOLVED by amendment.**
`labels: ['coderabbit']` → `[coderabbit]`, forced by yamllint's `quoted-strings: only-when-needed` (`.trunk/configs/.yamllint.yaml`) and criterion 4's repo-wide green gate. Criteria 4 and 10 contradicted each other. The operator accepted the edit; guard amended the In-scope list. Note the direction: the plan was wrong about reality, not the work wrong about the plan — the only condition under which scope may widen.

**F4 — `Planned at` SHA was on the wrong branch. RESOLVED by amendment.**
`8491bd3` lives on `feat/svg-motion-value-attributes` and was never an ancestor of this branch (cut from `a023d51`). The prescribed drift check therefore diffed against unrelated work. Harmless in the event — `main:eslint.config.mjs:31` matched the plan's "Current state" excerpt — and now re-stamped to `21d869c`.

**F5 — The plan made its own done criterion unsatisfiable. RESOLVED by amendment. (new this checkpoint)**
The plan file, committed at `561c9b8`, violates `prettier` and `markdownlint/MD060`, so `trunk check --all` could not exit 0 while it existed — and the executor may not edit the plan to fix it. `trunk check --all` exited **1** at `21d869c`, and (identical plan blob `4068732`) at `2b37049` too. Guard ran `trunk fmt` over the plan and guard artifacts; it now exits 0. Formatting only, no wording changed.

**Guard's own corrections.** Two claims in the checkpoint-1 report were wrong and are retracted:

1. "`trunk check --all` → exit 0." Never verified — the command piped into `tail` then `echo`, so `$?` captured the echo. It was exiting 1 (F5). Both executor commit bodies assert exit 0; that assertion was false, and guard repeated it instead of reproducing it. Every exit code in this report was read directly.
2. "14 `as`-style assertions still present." Inflated by a line-based classifier that could not tell _which_ assertion on a line was flagged — `animate(element!, snapshot as DOMKeyframesDefinition)` flags the `!`, not the `as`. The true count was the executor's 11. The finding's substance held; its number did not.

## Residual risk / follow-ups

- ~~**The pre-commit hook rewrites library source.**~~ **RETRACTED 2026-07-09 — this was guard's error, not a defect.** An earlier revision of this report claimed `.husky/pre-commit`'s `trunk check --fix` rewrote `src/lib/html/_MotionContainer.svelte`, stripping `element!` assertions despite `no-unnecessary-type-assertion` being `off` for `**/*.svelte`. That was never reproduced, and it is false. Run on a clean tree, `trunk check --fix` (the hook's exact command, no path args) exits 0, reports "No issues", and leaves the file's blob at `e1e1460` — byte-identical. Trunk honors the rule-off correctly. What actually happened: a **concurrent executor session** was writing to that file during guard's commit, running the raw `eslint --fix` measurement that its own commit (`21d869c`) documents — the one that strips all 35 `element!` assertions to count the resulting 19 svelte-check errors. Trunk's `discarded as modified during check [eslint]` means it detected the file changing _while it checked_ and discarded its results, which is trunk behaving **correctly** under a racing writer; the non-zero exit is why the commit aborted. No hook change is warranted. Do not remove `--fix`.
- **Latent sharp edge in the hook (real, but not the above).** `.husky/pre-commit` runs `trunk check --fix` with no path arguments, then restages only the originally-staged files. If `--fix` ever did fix an _unstaged_ file, that fix would land in the working tree silently and uncommitted. It fixes nothing today because the tree is lint-clean. Scoping the hook's `--fix` to staged files would harden it. Low priority, out of this plan's scope, and **not** a reason to drop `--fix` — that would turn autofixable nits into hard commit failures.
- **A rail is down in library source.** `no-unsafe-assignment` is off for **all** `**/*.svelte` (`eslint.config.mjs:142`), not just tests, against the plan's "library source keeps these rules". Forced back on across `src/lib/**/*.svelte` with a validated instrument: **0 hits** — nothing is hidden today, but future library `.svelte` code can accumulate unsafe assignments silently. Criterion 5 permits this given disclosure, and it was disclosed. Revisit if Trunk's `$props()` type-info degradation is fixed upstream.
- **Root configs and `scripts/**`get no type-aware linting.**`allowDefaultProject`cannot work under Trunk; those files fall into the`disableTypeChecked`guard. Confirmed directly — forcing a typed rule on`playwright.config.ts`throws "requires type information". Outside the plan's`Why this matters`, so acceptable, but it is a real coverage boundary and belongs in the plan's Maintenance notes.
- **Unverified claim, retained.** The new comment states that applying all 35 non-null fixes yields 19 svelte-check errors. Guard did not reproduce this — it requires mutating source. It is consistent with `8e177d4`'s body and is illustrative; the rule-off rests on "some of the 35 break the build", which that commit demonstrated.
- **Three pre-existing un-annotated `void` promises** in demo routes (`transform-template` ×2, `animate-presence/basic` ×1). Out of scope here; a one-line cleanup for whoever next touches those files.
- **Merge ordering.** Land this before `upstream-parity-top3` plans 003/004, which touch `drag.ts` and the projection/layout code this plan rewrote.
