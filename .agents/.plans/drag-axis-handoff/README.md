# Implementation Plans — drag-axis-handoff

Generated by the improve skill on 2026-07-26, planned at commit `41b2b20`.
Pre-merge addendum to PR #459 (drag-single-writer batch, closed 5/5 PASS):
the Codex adversarial review surfaced two HIGH findings, guard-verified as
real port gaps vs upstream — drag start does not stop foreign axis
animations (upstream `VisualElementDragControls.ts:114`), and whileDrag
deactivation follows momentum instead of preceding it (upstream `:270-305`),
with cleanup that misses foreign interruption. Both repaid here red-first.

## Execution order & status

| Plan | Title                                     | Priority | Effort | Risk | Depends on | Status |
| ---- | ----------------------------------------- | -------- | ------ | ---- | ---------- | ------ |
| 001  | Axis ownership at start + release cleanup | P1       | M      | MED  | —          | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED | REJECTED

## Shared context

- Branch: `drag-single-writer` (open PR #459). No pushes until the reviewer
  gates; the push updates the PR.
- Constraint ledger applies (archived visual-element-core + drag batches);
  the `isSync` release contract from drag 003 is untouchable.
- Guard provenance for the findings is in this plan's Why section and the
  archived `drag-single-writer/002-*.guard-report.md` (the accepted ordering
  deviation this repays).
