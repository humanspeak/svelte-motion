# Implementation Plans — motion-13.2-effects

Generated by the improve skill on 2026-09-03, planned at commit `47b7149`
(branch `chore/upstream-motion-13.2.0`, which bumps `motion`/`motion-dom`
13.1.1 → 13.2.0 — every plan's precondition is that this bump is installed).

Initiative: bring **every consumer-facing piece of Motion 13.2.0** into this
package. The 13.2.0 diff (v13.1.1..v13.2.0, 16 commits) adds one feature
family — the *effect registry* (`animate.addEffect`, `createEffect` options,
`MotionValueState.step`, `threeEffect`, `vgpuEffect`) — plus internal spring /
inertia refactors that we inherit automatically. The plans below expose the
registry (001), document and demo it (002), publish the adapter subpaths
(003), and add the Three.js flagship example (004).

Selection note: this batch was written non-interactively; per the improve
skill's default, all vetted findings that survived the "worth doing" bar
became plans, and the rejected ones are listed at the bottom.

## Execution order & status

| Plan | Title                                                                      | Priority | Effort | Risk | Depends on | Status |
| ---- | -------------------------------------------------------------------------- | -------- | ------ | ---- | ---------- | ------ |
| 001  | Expose the effect registry (`animate.addEffect`, `createEffect`) publicly  | P1       | S      | LOW  | —          | DONE — PASS at `7d09c0d` (see guard report); plan amended once (generic signature instead of `any`) |
| 002  | Custom-effects demo route, e2e, docs page, docs example                    | P2       | M      | LOW  | 001        | DONE — PASS at `c46aee4` (see guard report); docs typecheck has 5 pre-existing errors outside scope |
| 003  | Re-export `motion/three` and `motion/vgpu` as package subpaths             | P2       | S      | LOW  | 001        | DONE — PASS at `315c929` (see guard report) |
| 004  | Three.js flagship example (`animate(mesh)` + shader uniform)               | P3       | M      | MED  | 002, 003   | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 002 and 003 both need 001: 002's demo calls `animate.addEffect`, which is a
  type error until 001 adds it to `SvelteMotionAnimate`; 003 reuses 001's
  `typeof x & (widened signature)` re-typing pattern and the `EffectValues` type.
- 004 needs 003 for the `@humanspeak/svelte-motion/three` import and 002 for
  the docs page / example scaffolding it extends. 002 and 003 can run in
  parallel.
- 004 is the only plan that adds dependencies (`three`, `@types/three`, dev
  only, both packages). Operator sign-off on the live demo precedes any PR.

## Shared context

- Verify commands: `pnpm check`, `pnpm test:only`, `pnpm exec vitest run <file>`,
  `pnpm exec playwright test <dir> --reporter=line`, `pnpm package`,
  `trunk fmt` / `trunk check` (lint authority — `.trunk/trunk.yaml`).
- Docs consume the built library: `pnpm build` at the root before
  `cd docs && pnpm dev`; clear `docs/node_modules/.vite` if stale.
- e2e server port is 4198; if occupied, use `PW_REUSE_SERVER=1`, never kill it.
- Upstream reference: `~/Github/motion` at tag `v13.2.0`
  (`packages/motion-dom/src/effects/utils/create-effect.ts`,
  `packages/motion-dom/src/animation/animate/effects.ts`,
  `packages/framer-motion/src/animation/animate/index.ts`,
  `packages/motion/src/three.ts`, `dev/html/src/three-effects.js`).

## Findings considered and rejected

- **vgpu docs example**: `vgpu` is a 0.3.1 "scaffold" package with no stable
  API; the subpath re-export (003) is enough until it reaches 1.0.
- **Spring generator "parity pass"** (flagged by the competitive-intel run):
  we import upstream's `spring` rather than porting it, so the 13.2.0 rewrite
  (memoised underdamped sampling, `Math.pow` → multiplication) is inherited
  with zero code change; unit + drag e2e suites passed on 13.2.0.
- **Inertia bounce velocity fix**: `dragInertia.ts` calls upstream `inertia`;
  the analytic-derivative change is inherited. Drag e2e passed 26/27 with the
  one failure re-run 4× green (timing flake in `snap-to-origin`).
- **`calcGeneratorDuration` / `pregenerateKeyframes` signature changes**:
  internal to upstream; nothing in `src/lib` imports them.
- **Free-function `addEffect` / `removeEffect` / `findEffect` / `animateEffectSubject` re-exports**:
  upstream documents the `animate.addEffect` form; the free functions are
  duplicate surface. Import from `motion-dom` if a rare consumer needs them.
- **`styleEffect` / `attrEffect` / `svgEffect` `.get()`**: upstream wraps them
  in `createSelectorEffect`, which strips `.get`; there is nothing to expose.
  Only `propEffect` carries it (handled in 001).
- **`useAnimate` scoped animate gaining `addEffect`**: upstream's scoped
  animate lacks it too; adding it would be a divergence.
- **Pre-existing, unrelated**: `dist/reorder.d.ts` is not emitted
  (`$$IsomorphicComponent` cannot be named) in both the local build and the
  published 0.10.0. Worth its own issue; not part of this initiative.
