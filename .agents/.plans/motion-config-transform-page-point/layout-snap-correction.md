# Approved layout/snap correction — 2026-09-08

User: “Please handle the above P2 then bring up the test page of what the error still is”. Reviewed baseline: `872a3f41`. This authorizes correcting the reviewed regression and necessary focused coverage; all previous verification gates remain.

The read-only review reproduced a new no-config regression combining `layout`, imperative `snapToCursor`, a completed drag, and a parent layout shift. Pointer (117.164,112), second snapped center (44.328,-66). Substituting `14046a5` drag implementation in the same browser consumer restored center (117.164,112). `MotionDomProjectionAdapter.refreshLayout()` strips transforms before measurement, but the new snap cache records the still-translated axis MotionValues. These do not describe the measured geometry.

First add focused browser coverage to the existing transform-page-point drag fixture/test, with real layout displacement and repeated snaps. Preserve every prior case and assertion. Guard must observe RED before runtime changes. Include zero and nonzero initial coordinates if practical. Then correct the represented-axis measurement cache in `src/lib/utils/drag.ts`; add focused unit coverage there. Keep public API/coordinate domains, no-config behavior, all 29 parity cases and the protected repeated-initial RED test. Do not force fresh pointerdown projection measurements or change projection/animation engines merely to bypass the cache. If a narrow projection measurement metadata change is mechanically necessary, report the exact proposal before changing it.

Allowed source/test paths for this correction: `src/lib/utils/drag.ts`, `src/lib/utils/drag.spec.ts`, `src/routes/tests/transform-page-point/drag/+page.svelte`, `e2e/drag/transform-page-point.spec.ts`. Guard owns this plan and evidence. No edits to axis-handoff, other tests, dependencies or unrelated files. No push/PR.

Guard runs RED, then full units, fresh app/package build, targeted gesture browser coverage, strict 29-case parity, root checks and authoritative formatting/lint. Preserve prior docs gates unless a new source issue warrants repeating them. Full browser gate remains unresolved at axis-handoff; do not move past that review checkpoint. Finally open `/tests/drag/axis-handoff?@isPlaywright=true` in T3 at section 3 and explain expected takeover to x=420 versus the intermittent x=0 failure; three unchanged reruns passed, so do not claim it reproduces reliably.
