const {chromium}=require('/Users/jasonkummerl/Github/svelte-motion-transform-page-point/node_modules/@playwright/test');
(async()=>{const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1280,height:800}});const results=[];try{for(const zoom of ['50%','100%','150%','60 × 120']){await page.goto('http://127.0.0.1:5188/examples/transform-page-point');const tile=page.getByLabel('Draggable zoom-corrected tile');await tile.waitFor();await page.getByRole('button',{name:zoom,exact:true}).click();await tile.scrollIntoViewIfNeeded();await page.waitForTimeout(350);const a=await tile.boundingBox();const x=a.x+a.width/2,y=a.y+a.height/2;await page.mouse.move(x,y);await page.mouse.down();await page.mouse.move(x+40,y+25,{steps:10});await page.mouse.up();await page.waitForTimeout(300);const b=await tile.boundingBox();const dx=b.x+b.width/2-x,dy=b.y+b.height/2-y;const result={zoom,dx,dy,pass:Math.abs(dx-40)<2&&Math.abs(dy-25)<2};results.push(result);if(!result.pass)throw Error(JSON.stringify(result));}console.log(JSON.stringify(results,null,2));}finally{await browser.close();}})().catch(e=>{console.error(e);process.exitCode=1});
