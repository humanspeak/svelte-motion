# Implementation Plans — drag-single-writer

Generated by the improve skill on 2026-07-25, planned at `04418be` (re-stamped: `bb99032` was squashed away by PRs #454/#457; drag-surface files verified byte-identical to `7eba0bd`; container line references may have shifted with #457's svgEffect removal — locate by grep).
This is the RE-SCOPE of visual-element-core plan 005 (BLOCKED by sanctioned
deferral) — required reading: the archived plan + its revision notes and
guard log at `.agents/.plans-closed/visual-element-core/005-*.md`, which
record the two blockers, the banked findings, and the operator acceptance
criterion this batch resolves. The 002-batch monolith lesson is applied:
five small, independently-landable plans instead of one L-sized swap.

End state: drag is the last writer outside the VisualElement; after this
batch every element-style write in the library flows through one VE per
component, and hover responds during the post-release glide (the operator's
criterion).

## Execution order & status

| Plan | Title                                                           | Priority | Effort | Risk | Depends on | Status                                                                                                                                                                                                                                                                                                                            |
| ---- | --------------------------------------------------------------- | -------- | ------ | ---- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 001  | Drag axis writer through VE values (+ coalescing re-measure)    | P2       | M      | HIGH | —          | DONE — five-suite gate 178 passed / 2 skipped / 0 failed (baseline 177 + the new #458 parity pin); unit 801/801. Authored RAW transform strings resolved as upstream parity per the 2026-07-25 guard ruling (#458); the container's drag mirror measured INERT (0 writes / 0 mismatches over four pages) → plan 002 may delete it |
| 002  | whileDrag via setActive + global drag lock (+ glide-hover spec) | P2       | M      | MED  | 001        | TODO                                                                                                                                                                                                                                                                                                                              |
| 003  | Inertia + settle retargeted onto axis values                    | P2       | S      | MED  | 001        | TODO                                                                                                                                                                                                                                                                                                                              |
| 004  | whilePan through VE values (extension semantics)                | P3       | S      | MED  | 001        | TODO                                                                                                                                                                                                                                                                                                                              |
| 005  | Retire the legacy layout FLIP fallback                          | P3       | S      | MED  | 001        | TODO                                                                                                                                                                                                                                                                                                                              |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Dependency notes

- 001 is the heart: it rewrites the single write path (`setXYImmediate`)
  in place, so 002–005 all sit on it. It deliberately KEEPS all 18 call
  sites working (inertia/settle/origin/keyboard/constraints call the same
  function) — 003 then simplifies those callers.
- 002 delivers the operator acceptance criterion (hover during glide) and
  retires both pieces of plan-003 scaffolding (the container mirror, the
  dataset guard). The banked risk note applies: the global lock lands ONLY
  in the same plan as the whileDrag `setActive` work, never alone.
- 004 (whilePan) is a svelte-motion EXTENSION with no upstream
  AnimationType — it goes through VE values via `animateTarget`, NOT
  through the animationState's fixed type list. Do not fork animation-state.
- Whole batch remains P2/P3 and deferrable; drag behavior is
  operator-signed-off (#399–#415 and the #449 tour §7) — zero behavioral
  regressions is the bar, BLOCKED-with-clean-report beats a regression.

## Shared context for every executor

- Verify commands: `pnpm check`, `pnpm test:only`, `pnpm test:e2e <dirs>`,
  `trunk fmt` / `trunk check` (lint authority). Never push; never reset
  past commits you did not author.
- Upstream reference: `~/Github/motion/packages/framer-motion/src/gestures/drag/`
  (`VisualElementDragControls.ts` is the ported contract); installed truth
  `node_modules/motion-dom/dist/index.d.ts` (12.42.2).
- Constraint ledger applies
  (`.agents/.plans-closed/visual-element-core/002-*.md` revisions #4–#9):
  microtask flushes, never-skip-first-`animateChanges`, untracked adapter
  `updateOptions`, non-memoized style slots, TDZ ordering in the container,
  per-channel `value.stop()` for accelerated freezes.
- Verified fact to build on (banked 2026-07-25): a page-bound style
  MotionValue IS the node's axis value —
  `ve.values.get('y') === ve.props.style.y` — so `ve.getValue('x'|'y')` is
  the correct writer handle.

## Findings considered and rejected

- Landing the drag lock alone as a "cheap first slice": rejected — an
  unreleased global lock kills hover/press everywhere, while the dataset
  flag is per-element self-limiting; lock lands only with 002's setActive
  work (banked ruling).
- Mapping `whilePan` onto the `whileDrag` AnimationType slot: rejected —
  collides when both props are used and misrepresents priority semantics.
- Adopting upstream `VisualElementDragControls` wholesale: still rejected
  (React-shaped props/context); this batch ports the write-path contract.
