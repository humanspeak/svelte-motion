# Implementation Plans — svg-through-ve

Generated by the improve skill on 2026-07-25, planned at commit `4758dcd`.
Deferred from the visual-element-core batch (plan 002 Step 6, skipped by
guard ruling because `e2e/svg` was fully green and the migration was churn
mid-batch). This batch completes the single-writer story for SVG: attributes
and path drawing move onto the `SVGVisualElement` the elements already have.

## Execution order & status

| Plan | Title                                                  | Priority | Effort | Risk | Depends on                  | Status |
| ---- | ------------------------------------------------------ | -------- | ------ | ---- | --------------------------- | ------ |
| 001  | SVG attributes + path drawing through SVGVisualElement | P2       | M      | MED  | visual-element-core 001–004 | TODO   |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale)

## Shared context

- Verify commands: `pnpm check`, `pnpm test:only`, `pnpm test:e2e <dirs>`,
  `trunk fmt` / `trunk check` (lint authority).
- Upstream reference: `~/Github/motion` (motion-dom SVG render utils);
  installed truth: `node_modules/motion-dom/dist/index.d.ts` (12.42.2).
- The visual-element-core constraint ledger applies
  (`.agents/.plans/visual-element-core/002-*.md` revisions #4–#9).
- Operator sign-off precedes any push/PR.

## Findings considered and rejected

- Migrating `svgEffect`'s PUBLIC export onto a VE-backed implementation:
  out of scope — it is a standalone vanilla API whose contract is
  independent of the component pipeline.
